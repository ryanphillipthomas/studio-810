{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wearestudio810.com/contracts/brief.schema.json",
  "title": "Chief brief (10-brief.json)",
  "description": "Chief → Product. The interpreted objective and route plan for the run.",
  "type": "object",
  "required": ["runId", "objective", "successCriteria", "constraints", "route", "assumptions"],
  "additionalProperties": false,
  "properties": {
    "runId": { "type": "string" },
    "objective": { "type": "string", "minLength": 1, "description": "One sentence. What this run exists to produce." },
    "successCriteria": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "Measurable statements; QA's verdict ultimately traces back to these"
    },
    "constraints": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Hard limits beyond the standing guardrails (target app, deadline, must-use components)"
    },
    "targetApp": { "enum": ["connect", "studio810-site", "shared"], "description": "Which part of the umbrella this run touches" },
    "route": {
      "type": "object",
      "required": ["skipResearch"],
      "additionalProperties": false,
      "properties": {
        "skipResearch": { "type": "boolean" },
        "skipReason": { "type": "string", "description": "Required in prose when skipResearch=true" }
      }
    },
    "assumptions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Ambiguities in the prompt and the interpretation chosen. Empty array means the prompt was unambiguous."
    }
  }
}
