# wearestudio810.com — umbrella root

The root of wearestudio810.com — the public site of Studio 810, a live/work creative studio in New York. Currently a placeholder hero linking to the design-system POC at `/poc/`; the real site is built by the pipeline from the DS-001 foundation when that work is ticketed. Served at `wearestudio810.com/` via `tools/site-compose` (see `render.yaml`).
