# wearestudio810.com — umbrella root

The root of wearestudio810.com. Currently a placeholder redirect to `/skills/connect/`; the real marketing site is built by the pipeline from the DS-001 foundation when that work is ticketed. Served at `wearestudio810.com/` via `tools/site-compose` (see `render.yaml`).
